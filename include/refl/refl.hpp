// refl — C++26 compile-time-reflection-based runtime reflection framework.
//
// Design goal (see docs/design.md): wrap a class as Refl<MyClass> so it
// registers in a global pool queryable at runtime via find_class("MyClass"),
// then construct objects and call members through reflected handles.
//
// Skeleton placeholder — implementation pending.
#pragma once

namespace refl {
// Framework API will live here.
}  // namespace refl
