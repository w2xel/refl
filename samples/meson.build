# Samples — runnable usage examples.
# Built but not tested; they exist to demonstrate the intended API.

inc = include_directories('../include')

executable(
  'basic_sample',
  'basic.cpp',
  include_directories: inc,
  install: false,
)
