// Sample: intended usage of the refl framework.
//
// The API below is aspirational (see README.md and docs/design.md); the
// framework is not yet implemented. This file exists to anchor the samples
// layout and will be filled in as Reffl<MyClass> lands.
#include <refl/refl.hpp>

int main() {
    // Target shape, once implemented:
    //   refl::Refl<MyClass> obj;
    //   auto cls = *refl::find_class("MyClass");
    //   auto construct = *cls.find_constructor("int", "int");
    //   auto instance = construct.call(1, 3);
    (void)0;
    return 0;
}
