// Placeholder unit test — verifies the framework header is includable.
// Real tests will be added as the framework is implemented.
#include <refl/refl.hpp>

int main() {
    // Empty: just confirms the header compiles under the project's flags.
    return 0;
}
